import React, {Component} from 'react';
import {Col, Container, Nav, NavItem, NavLink, Row, TabContainer} from "react-bootstrap";

export default class Map extends Component {
    render() {
        return (
            <></>
        );
    }
}
